import { Button } from "@/ui/components/button";

export default function Page() {
  return (
    <div className="w-full h-full p-10 flex flex-col gap-5">
      <Button variant="brand">Click</Button>
    </div>
  );
}
